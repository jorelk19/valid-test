<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.valid.edson" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name="com.valid.di.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Valid" >

        <activity android:name="com.valid.edson.ui.views.activities.splashScreen.SplashScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.valid.edson.ui.views.activities.MainActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.Valid"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.valid.edson.ui.views.activities.SignInActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.Valid"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name="com.valid.edson.ui.views.activities.NotificationActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Translucent"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>